import { expect } from 'chai';

describe('thefrankenste.in', () => {
  it('should be fun', () => {
    const FUN = true;

    expect(FUN).to.be.true; // eslint-disable-line no-unused-expressions
  });
});
